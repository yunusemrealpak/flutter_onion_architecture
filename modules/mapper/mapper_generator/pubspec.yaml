name: mapper_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  dart_style: ^2.3.3
  glob: ^2.1.2
  build: ^2.4.1
  build_config:
  source_gen: ^1.4.0
  mapper_annotation:
    path: ../mapper_annotation/

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
